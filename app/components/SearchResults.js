import { useContext } from "react";
import classNames from "classnames";

import { PlexContext } from "./App";

const SearchResults = ({searchData}) => {


    return (
        // {searchData.map((title, i) =>
        //     <div className="">{title.title}</div>
        // )}
        <div className=""></div>
    )

}

export default SearchResults;